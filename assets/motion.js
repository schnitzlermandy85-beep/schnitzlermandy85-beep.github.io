(()=>{const reduced=matchMedia('(prefers-reduced-motion: reduce)'),fine=matchMedia('(hover: hover) and (pointer: fine)');const cards=[...document.querySelectorAll('.cover-card,.hello-card,.clock-card,.month-card,.feed,.editor,.manage')];if(!reduced.matches&&'IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.remove('reveal-pending');e.target.classList.add('reveal-visible');observer.unobserve(e.target)}}),{threshold:.05});cards.forEach((card,i)=>{card.classList.add('reveal-pending');card.style.transitionDelay=`${Math.min(i%4*65,195)}ms`;observer.observe(card)});reduced.addEventListener('change',()=>{if(reduced.matches){cards.forEach(c=>{c.classList.remove('reveal-pending');c.style.transitionDelay='0ms'});observer.disconnect()}})}cards.forEach(card=>{card.classList.add('glass-reactive');let frame=0;card.addEventListener('pointermove',e=>{if(!fine.matches||reduced.matches)return;cancelAnimationFrame(frame);frame=requestAnimationFrame(()=>{const r=card.getBoundingClientRect();card.style.setProperty('--shine-x',`${e.clientX-r.left}px`);card.style.setProperty('--shine-y',`${e.clientY-r.top}px`)})});card.addEventListener('pointerleave',()=>cancelAnimationFrame(frame))});const tabs=document.querySelector('.tabs');if(tabs){const pill=document.createElement('span');pill.className='tab-glass';pill.setAttribute('aria-hidden','true');tabs.prepend(pill);const sync=()=>{const b=tabs.querySelector('[aria-pressed="true"]');if(b){pill.style.width=b.offsetWidth+'px';pill.style.transform=`translateX(${b.offsetLeft}px)`}};tabs.addEventListener('click',()=>requestAnimationFrame(sync));addEventListener('resize',sync);sync()}document.querySelectorAll('.topbar nav a').forEach(a=>{const u=new URL(a.href);if(u.origin===location.origin&&u.pathname===location.pathname&&u.search===location.search){a.classList.add('active');a.setAttribute('aria-current','page')}});document.addEventListener('pointerdown',e=>{if(!fine.matches||reduced.matches||e.button!==0||e.target.closest('input,textarea,select'))return;const r=document.createElement('span');r.className='tap-ripple';r.style.left=e.clientX+'px';r.style.top=e.clientY+'px';r.setAttribute('aria-hidden','true');document.body.append(r);setTimeout(()=>r.remove(),700)});})();
